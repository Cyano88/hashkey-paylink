// Local synthetic UI preview. Does not call live identity, project or payment APIs.
import {build} from 'esbuild'
import fs from 'node:fs/promises'
import {createServer} from 'node:http'
import {resolve,extname,sep} from 'node:path'
import {spawnSync} from 'node:child_process'
const port=Number(process.env.DEVELOPER_PREVIEW_PORT??4319)
const root=process.cwd(),out=resolve(root,'output/playwright/developer-preview')
await fs.mkdir(out,{recursive:true})
const entry=`import React from 'react';import {createRoot} from 'react-dom/client';import {BrowserRouter,Routes,Route} from 'react-router-dom';import DeveloperLayout from './src/surfaces/DeveloperLayout';import Portal from './src/pages/DeveloperPortalPage';
const base={id:'dev_preview12345678',name:'Builder demo',ownerEmail:'builder@example.test',website:'https://example.test',brandImageUrl:'',useCase:'Payments for an example product.',checkoutMode:'human',capabilities:['hosted_checkout','arc_agreements'],settlementMode:'usdc',settlementStatus:'ready',operationalStatus:'active',networks:['arc'],defaultNetwork:'arc',recipients:{arc:'0x1111111111111111111111111111111111111111'},refundAddress:'',allowedOrigins:['https://example.test'],webhookUrl:'https://example.test/webhook',webhookConfigured:true,bankCode:'',bankName:'',bankAccountName:'',bankAccountLast4:'',keys:[],updatedAt:'2026-09-24T12:00:00Z',webhookDeliveries:[{id:'evt_preview123456',event:'payment.confirmed',status:'delivered',responseStatus:200,attemptedAt:'2026-09-24T12:00:00Z'}]};if(new URLSearchParams(location.search).has('agent'))Object.assign(base,{checkoutMode:'agentic',networks:['arbitrum','arc'],defaultNetwork:'arbitrum',recipients:{...base.recipients,arbitrum:'0x1111111111111111111111111111111111111111'}});let projects=[base,{...base,id:'dev_funding12345678',name:'Funding demo',capabilities:['polymarket_funding'],networks:['base'],defaultNetwork:'base',webhookDeliveries:[]}];window.__portalWrites=[];
window.fetch=async(path,options={})=>{if(!String(path).startsWith('/api/developer-projects'))throw Error('Only fixture API is allowed');if(new URLSearchParams(location.search).has('fail'))return new Response(JSON.stringify({ok:false,error:'Synthetic network failure'}),{status:503});const q=new URL(String(path),'http://localhost').searchParams;if(q.get('resource')==='activity'){if(new URLSearchParams(location.search).has('activityFail'))return Response.json({ok:false,error:'Synthetic activity unavailable'},{status:503});const match=q.get('projectId')==='dev_preview12345678'&&(!q.get('recordId')||q.get('recordId')==='chk_preview123456');const cursor=q.get('cursor');return Response.json({ok:true,events:match?[{id:cursor?'history_created':'history_paid',cursor:cursor?'9':'10',projectId:q.get('projectId'),environment:'live',product:'checkout',recordId:'chk_preview123456',event:cursor?'checkout.created':'payment.paid',occurredAt:'2026-09-24T12:00:00Z',recordedAt:'2026-09-24T12:00:02Z',details:{amount:'10',asset:'USDC',network:'arc',evidence:'payment_verifier',txHash:'0x1111111111111111111111111111111111111111111111111111111111111111'}}]:[],nextCursor:match&&!cursor?'10':null,coverage:'Synthetic lifecycle observations'})}if(options.method==='POST'){const body=JSON.parse(options.body);if(['create-key','rotate-webhook-secret'].includes(body.action)){window.__portalWrites.push({action:body.action,projectId:body.projectId});if(new URLSearchParams(location.search).has('secretDelay'))await new Promise(resolve=>{window.__resolveSecret=resolve});return Response.json({ok:true,apiKey:'SYNTHETIC_KEY_FOR_'+body.projectId,webhookSecret:'SYNTHETIC_WEBHOOK_FOR_'+body.projectId})}}if(options.method==='PUT'){const body=JSON.parse(options.body);window.__portalWrites.push(body);projects=projects.map(p=>p.id===body.projectId?{...p,...body,updatedAt:new Date().toISOString()}:p);return Response.json({ok:true,project:projects.find(p=>p.id===body.projectId)})}return Response.json({ok:true,projects})};
createRoot(document.getElementById('root')).render(<BrowserRouter><Routes><Route element={<DeveloperLayout/>}><Route path='*' element={<Portal/>}/></Route></Routes></BrowserRouter>);`
await build({stdin:{contents:entry,resolveDir:root,loader:'tsx'},outfile:resolve(out,'app.js'),bundle:true,format:'esm',platform:'browser',jsx:'automatic',define:{'process.env.NODE_ENV':'"development"'},plugins:[{name:'mock-identity',setup(b){b.onResolve({filter:/^@privy-io\/react-auth$/},()=>({path:'privy',namespace:'fixture'}));b.onLoad({filter:/.*/,namespace:'fixture'},()=>({contents:`export const usePrivy=()=>({ready:true,authenticated:true,getAccessToken:async()=>'synthetic-preview-token',logout:async()=>{}});`,loader:'js'}));b.onLoad({filter:/PocketEmailLogin\.tsx$/},()=>({contents:'export default function Login(){return null}',loader:'tsx'}))}}]})
const css=spawnSync(process.execPath,['node_modules/tailwindcss/lib/cli.js','-i','src/index.css','-o',resolve(out,'styles.css')],{cwd:root,encoding:'utf8'});if(css.status!==0)throw Error(css.stderr)
await fs.writeFile(resolve(out,'index.html'),'<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Developer portal - synthetic preview</title><link rel="stylesheet" href="/styles.css"><link rel="stylesheet" href="/app.css"></head><body><div id="root"></div><script type="module" src="/app.js"></script></body></html>')
if (!process.argv.includes('--build-only')) createServer(async(req,res)=>{try{const pathname=decodeURIComponent(new URL(req.url,'http://localhost').pathname);let file=resolve(out,'.'+pathname);if(!file.startsWith(out+sep))file=resolve(out,'index.html');if(!extname(file))file=resolve(out,'index.html');res.setHeader('Content-Type',({'.js':'text/javascript','.css':'text/css','.html':'text/html'})[extname(file)]||'application/octet-stream');res.end(await fs.readFile(file))}catch{res.statusCode=404;res.end('Not found')}}).listen(port,'127.0.0.1',()=>console.log('Synthetic portal preview: http://127.0.0.1:'+port))
