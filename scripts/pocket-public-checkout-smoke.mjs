import {build} from 'esbuild';import fs from 'node:fs';import assert from 'node:assert/strict';
fs.mkdirSync('.codex-temp',{recursive:true});
await build({entryPoints:['api/pocket/checkout-config.ts'],outfile:'.codex-temp/config-check.mjs',bundle:true,platform:'node',format:'esm',packages:'external'});
const {default:config}=await import('../.codex-temp/config-check.mjs');const read=()=>{let result;config({}, {setHeader(){},json(d){result=d}});return result.playStoreUrl};const previous=process.env.POCKET_PLAY_STORE_URL;
try{delete process.env.POCKET_PLAY_STORE_URL;assert.equal(read(),'');process.env.POCKET_PLAY_STORE_URL='https://malicious.invalid/store/apps/details?id=com.hashpaylink.pocket';assert.equal(read(),'');process.env.POCKET_PLAY_STORE_URL='https://play.google.com/store/apps/details?id=other';assert.equal(read(),'');process.env.POCKET_PLAY_STORE_URL='https://play.google.com/store/apps/details?id=com.hashpaylink.pocket';assert.equal(read(),process.env.POCKET_PLAY_STORE_URL)}finally{if(previous===undefined)delete process.env.POCKET_PLAY_STORE_URL;else process.env.POCKET_PLAY_STORE_URL=previous}
await build({entryPoints:['src/pages/NigerianPos.tsx'],outfile:'.codex-temp/pos-url-check.mjs',bundle:true,platform:'node',format:'esm',packages:'external'});
const {publicPosCheckoutUrl}=await import('../.codex-temp/pos-url-check.mjs');const origin='https://app.hashpaylink.com';const base={merchant_id:'fixture',display_name:'Fixture shop',bank_configured:false,payout_preference:'KEEP_CRYPTO',supported_networks:['base'],circle_smart_wallet_address:'0x'+'1'.repeat(40)};
let url=new URL(publicPosCheckoutUrl(base,origin));assert.equal(url.pathname,'/pay');assert.equal(url.searchParams.get('merchant'),'fixture');assert.equal(url.searchParams.get('src'),'ngpos');assert.equal(url.searchParams.get('e'),base.circle_smart_wallet_address);assert.equal(url.searchParams.get('f'),'1');
url=new URL(publicPosCheckoutUrl({...base,bank_configured:true,payout_preference:'INSTANT_FIAT'},origin));assert.equal(url.searchParams.get('n'),'base');assert.equal(url.searchParams.get('offramp'),'paycrest');assert.equal(url.searchParams.get('fx'),'NGN');assert.equal(url.searchParams.has('e'),false);
console.log('PASS store CTA remains disabled without valid Pocket listing; Stablecoins POS links map to existing Circle payment routes');
