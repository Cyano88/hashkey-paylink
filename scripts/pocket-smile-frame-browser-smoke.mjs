import assert from 'node:assert/strict'
import {build} from 'esbuild'
const {chromium}=await import(process.env.PLAYWRIGHT_MODULE??'playwright')
const bundle=await build({stdin:{contents:"import{openSmileFrame}from'./src/pocket/lib/smileFrame';window.openSmileFrame=openSmileFrame",resolveDir:process.cwd(),loader:'ts'},bundle:true,write:false,format:'iife'})
const browser=await chromium.launch({headless:true,channel:'chrome'})
try {
 const page=await browser.newPage()
 await page.route('https://app.hashpaylink.com/',r=>r.fulfill({contentType:'text/html',body:'<body></body>'}))
 await page.route('https://hashkey-paylink.onrender.com/pocket/identity-frame**',r=>r.fulfill({contentType:'text/html',body:`<script>window.addEventListener('message',e=>window.received=e.data);parent.postMessage('SmileIdentity::ChildPageReady','https://app.hashpaylink.com')</script>`}))
 await page.goto('https://app.hashpaylink.com/')
 await page.addScriptTag({content:bundle.outputFiles[0].text})
 await page.evaluate(()=>{window.success=0;window.closeCalls=0;window.errors=0;window.openSmileFrame({token:'fixture-secret',onSuccess:value=>{window.success++;window.submission=value},onClose:()=>window.closeCalls++,onError:()=>window.errors++})})
 const frame=page.frames().find(f=>f.url().includes('/identity-frame'))??await new Promise(resolve=>page.once('framenavigated',resolve))
 await frame.waitForFunction(()=>!!window.received)
 assert.equal(await frame.evaluate(()=>JSON.parse(window.received).token),'fixture-secret')
 assert.equal(new URL(frame.url()).searchParams.get('capture'),'v12-recovery-20260923')
 assert.equal(new URL(frame.url()).searchParams.has('token'),false)
 await page.evaluate(()=>{
  const source=document.querySelector('iframe').contentWindow
  window.dispatchEvent(new MessageEvent('message',{origin:'https://evil.invalid',source,data:'SmileIdentity::Success'}))
  window.dispatchEvent(new MessageEvent('message',{origin:'https://hashkey-paylink.onrender.com',source:window,data:'SmileIdentity::Success'}))
  window.dispatchEvent(new MessageEvent('message',{origin:'https://hashkey-paylink.onrender.com',source,data:{message:42}}))
 })
 assert.equal(await page.evaluate(()=>window.success),0)
 await frame.evaluate(()=>parent.postMessage({message:'SmileIdentity::Error',status:400,error_code:'SUBMISSION_FAILED',retryable:false,error:'id_number is invalid'},'https://app.hashpaylink.com'))
 await page.waitForFunction(()=>window.errors===1)
 assert.equal(await page.locator('iframe').count(),1,'Submission rejection must keep provider screen open')
 assert.equal(await page.evaluate(()=>JSON.parse(sessionStorage.getItem('pocket:kyc-error:v1')).status),400)
 await frame.evaluate(()=>parent.postMessage({message:'SmileIdentity::Result',payload:{status:'failure',error:{status:403,error_code:'NOT_PERMITTED',retryable:false}}},'https://app.hashpaylink.com'))
 await page.waitForFunction(()=>window.errors===2)
 assert.equal(await page.evaluate(()=>window.success),0,'Failure Result cannot become submitted')
 await frame.evaluate(()=>parent.postMessage({message:'SmileIdentity::Result',payload:{status:'Accepted'}},'https://app.hashpaylink.com'))
 await page.waitForTimeout(50);assert.equal(await page.evaluate(()=>window.success),0,'Malformed Result cannot become submitted')
 await frame.evaluate(()=>{parent.postMessage({message:'SmileIdentity::Result',payload:{status:'Accepted',job_id:'job_'+ '0'.repeat(26),user_id:'user_fixture'}},'https://app.hashpaylink.com');parent.postMessage('SmileIdentity::Success','https://app.hashpaylink.com')})
 await page.waitForFunction(()=>window.success===1)
 await frame.evaluate(()=>parent.postMessage('SmileIdentity::Close::System','https://app.hashpaylink.com'))
 await page.waitForFunction(()=>!document.querySelector('iframe'))
 assert.deepEqual(await page.evaluate(()=>[window.success,window.closeCalls,window.errors]),[1,0,2])
 assert.deepEqual(await page.evaluate(()=>window.submission),{jobId:'job_'+'0'.repeat(26),userId:'user_fixture'})
 await page.evaluate(()=>window.openSmileFrame({onClose:()=>window.closeCalls++,onSuccess:()=>window.success++}))
 await page.waitForSelector('iframe');const cancelled=page.frames().find(f=>f.url().includes('/identity-frame'));await cancelled.waitForFunction(()=>!!window.received);await cancelled.evaluate(()=>parent.postMessage({message:'SmileIdentity::Result',payload:{status:'cancelled'}},'https://app.hashpaylink.com'));await page.waitForFunction(()=>window.closeCalls===1);assert.equal(await page.evaluate(()=>window.success),1)
 console.log('PASS V12 raw Accepted, failure/cancelled Result handling, accepted references, malformed rejection, trusted frame handshake, token excluded from URL, spoofed messages rejected, malformed messages ignored, one upload callback, and cleanup.')
} finally {await browser.close()}
