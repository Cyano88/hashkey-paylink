import assert from 'node:assert/strict'
import {build} from 'esbuild'
const {chromium}=await import(process.env.PLAYWRIGHT_MODULE||'playwright')
const stubs={pocketActivityCache:'export const markPocketActivityDirty=()=>{};',pocketBillsClient:`const available={enabled:true,environment:'live',airtimeEnabled:true};export const processPocketBillRefund=async()=>{throw Error('Refund not expected in this fixture')};export class PocketBillsApiError extends Error{};export const cachedPocketBillsAvailability=()=>available;export const readPocketBillsAvailability=async()=>available;const intent=()=>({id:'bill-fixture',state:'quoted',quoteExpiresAt:Date.now()+60000,treasuryAddress:'0x'+'2'.repeat(40),amountUsdc:'1'});export const quotePocketAirtime=async()=>({intent:intent()});export const quotePocketData=quotePocketAirtime,quotePocketTv=quotePocketAirtime,quotePocketElectricity=quotePocketAirtime;export const preparePocketAirtime=async()=>intent();export const confirmPocketAirtime=async()=>({...intent(),state:'delivered'});export const refreshPocketAirtime=async()=>({...intent(),state:'delivered'});export const readPocketDataCatalog=async()=>({services:[],variations:[]});export const verifyPocketBillCustomer=async()=>({});`,pocketEvmTransferClient:`export const executePocketEvmTransfer=async()=>{window.sends++;return {txHash:'0x'+'3'.repeat(64),status:'confirmed'}}`,pocketPaymentApproval:'export const registerPocketPaymentPreparer=()=>()=>{};',pocketRefresh:'export const registerPocketRefreshHandler=()=>()=>{};'}
const bundle=await build({stdin:{contents:`import React,{useState}from'react';import{createRoot}from'react-dom/client';import useBills from './src/pocket/controllers/usePocketBillsController';const wallet={address:'0x'+'1'.repeat(40)};const token=async()=> 'fixture',refresh=async()=>{},session=()=>new Promise(r=>window.release=()=>r({wallet}));window.sends=0;function App(){const[owner,setOwner]=useState('alice');window.owner=setOwner;const bills=useBills({owner,view:'airtime',authenticated:true,baseWallet:wallet,getAccessToken:token,ensureBaseWallet:async()=>wallet,getEvmSession:session,refreshBalances:refresh});window.bills=bills;return <output>{owner}:{bills.status}</output>}createRoot(document.getElementById('app')).render(<App/>);`,loader:'tsx',resolveDir:process.cwd()},bundle:true,write:false,format:'iife',define:{'process.env.NODE_ENV':'"test"'},plugins:[{name:'stubs',setup(b){b.onResolve({filter:/.*/},a=>{const key=a.path.split('/').pop();if(stubs[key])return{path:key,namespace:'stub'}});b.onLoad({filter:/.*/,namespace:'stub'},a=>({contents:stubs[a.path]}))}}]})
const browser=await chromium.launch({headless:true,channel:'chrome'})
try{for(const scenario of ['duplicate','switch']){const page=await browser.newPage();await page.route('https://fixture.invalid/',r=>r.fulfill({contentType:'text/html',body:'<div id="app"></div>'}));await page.goto('https://fixture.invalid/');await page.addScriptTag({content:bundle.outputFiles[0].text});await page.waitForFunction(()=>!!window.bills);await page.evaluate(()=>window.bills.review());await page.waitForFunction(()=>window.bills.status==='ready');await page.evaluate(()=>{window.bills.pay();window.bills.pay()});await page.waitForFunction(()=>!!window.release);if(scenario==='switch'){await page.evaluate(()=>window.owner('bob'));await page.waitForFunction(()=>document.querySelector('output').textContent==='bob:idle')}await page.evaluate(()=>window.release());if(scenario==='duplicate'){await page.waitForFunction(()=>window.bills.status==='successful');assert.equal(await page.evaluate(()=>window.sends),1);await page.evaluate(()=>window.bills.dismiss());await page.waitForFunction(()=>window.bills.status==='idle')}else{await page.waitForTimeout(100);assert.equal(await page.evaluate(()=>window.sends),0);assert.equal(await page.locator('output').textContent(),'bob:idle')}await page.close()}console.log('PASS bills reject duplicate approval calls, account switch stops submission after session resolves, provider delivery succeeds, Done clears form. Mock requests only.')}finally{await browser.close()}
